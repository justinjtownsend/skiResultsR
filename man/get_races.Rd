% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/races.R
\name{get_races}
\alias{get_races}
\title{Extract all race tables from an event HTML file}
\usage{
get_races(file_path)
}
\arguments{
\item{file_path}{Path to the HTML file containing event results}
}
\value{
A list where each element is a race table containing:
\describe{
\item{race_id}{The race identifier (e.g., "race-9973")}
\item{race_data}{Data frame with race results}
\item{metadata}{Additional race metadata if available}
}

A named list of data frames, where each element is named by race_id
and contains the race results from get_race()
}
\description{
This function extracts all race tables from a skiresults.co.uk event HTML file.
It returns structured data with core race information, additional points,
racer and club information for all races in the event.
}
\examples{
\dontrun{
# Extract all race tables
file_path <- system.file("extdata", "chatham_oct2023.html", package = "skiResultsR")
races <- get_races(file_path)

# View race IDs
names(races)

# Access specific race
first_race <- races[[1]]
head(first_race$race_data)
}
Extract all races from an event HTML file

This function extracts all race results from a skiresults.co.uk event HTML file.
It uses get_race_types() to get the list of race IDs, then calls get_race()
for each race ID to extract the complete race results.

\dontrun{
# Extract all races from an event
file_path <- system.file("extdata", "chatham_oct2023.html", package = "skiResultsR")
races <- get_races(file_path)

# Access specific race by ID
names(races)
races[["race-9973"]]
}
}
