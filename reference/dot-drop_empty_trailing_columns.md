# Internal function to drop empty trailing columns

Rule 9: Drop empty trailing columns (from ignored Points data) Removes
columns from the end of the data frame that are completely empty

## Usage

``` r
.drop_empty_trailing_columns(df)
```

## Arguments

- df:

  Data frame

## Value

Data frame with empty trailing columns removed
