# Internal function to parse date string to ISO format

Internal function to parse date string to ISO format

## Usage

``` r
.parse_date_string(date_string)
```

## Arguments

- date_string:

  Date string in various formats (e.g., "07 October 2023")

## Value

ISO date string (YYYY-MM-DD) or NA if parsing fails
